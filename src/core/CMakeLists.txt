### Build target
set(SOURCES
    exc_cor.f90
)

add_library(${SALMON_CORE_LIB} STATIC ${SOURCES})
list(APPEND SALMON_LINK_LIBRARIES ${SALMON_CORE_LIB})
set(SALMON_LINK_LIBRARIES ${SALMON_LINK_LIBRARIES} PARENT_SCOPE)
