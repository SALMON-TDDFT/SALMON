set(SOURCES
    input_pp.f90
    salmon_pp.f90
   )

add_library(${SALMON_PP_LIB} STATIC ${SOURCES})
add_dependencies(${SALMON_PP_LIB} ${SALMON_PARALLEL_LIB} ${SALMON_IO_LIB})
list(APPEND SALMON_LINK_LIBRARIES ${SALMON_PP_LIB})
set(SALMON_LINK_LIBRARIES ${SALMON_LINK_LIBRARIES} PARENT_SCOPE)
