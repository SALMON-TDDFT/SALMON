set(SOURCES
    salmon_math.f90
   )

add_library(${SALMON_MATH_LIB} STATIC ${SOURCES})
#add_dependencies(${SALMON_MATH_LIB} ${SALMON_PARALLEL_LIB})
list(APPEND SALMON_LINK_LIBRARIES ${SALMON_MATH_LIB})
set(SALMON_LINK_LIBRARIES ${SALMON_LINK_LIBRARIES} PARENT_SCOPE)
