set(SOURCES
   )

add_library(${SALMON_POISSON_LIB} STATIC ${SOURCES})
add_dependencies(${SALMON_POISSON_LIB} ${SALMON_PARALLEL_LIB} ${SALMON_IO_LIB})
list(APPEND SALMON_LINK_LIBRARIES ${SALMON_POISSON_LIB})
set(SALMON_LINK_LIBRARIES ${SALMON_LINK_LIBRARIES} PARENT_SCOPE)
